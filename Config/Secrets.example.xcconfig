// Copy to Config/Secrets.xcconfig (gitignored) and fill in.
//
// The publishable key is designed to live in client code and is protected by
// RLS — anon holds zero privileges in this project, verified. It still doesn't
// belong in git, because rotating a key that's in history is a chore.
//
// Find both in the Supabase dashboard under Settings → API.

SUPABASE_URL = https:/$()/zqnzjotcdgqghbiteiso.supabase.co
SUPABASE_PUBLISHABLE_KEY = sb_publishable_REPLACE_ME
